# `mild-lyrics/copytest.py`

Comments lifted out of `mild-lyrics/copytest.py`. Docstrings stayed in the code, and so did tool directives (`noqa`, `pragma`, the shebang).


## module level

**line 29** — before `ROOT = HERE.parent`

> Data lives beside the code's folder, not inside it.


## `main`

**line 92** — before `LS.pin_source(tid, old)`

> Put it back exactly as it was, whatever happened above. This is
> an experiment, not a change of copy.
