# `editor/backups.py`

Comments lifted out of `editor/backups.py`. Docstrings stayed in the code, and so did tool directives (`noqa`, `pragma`, the shebang).


## module level

**line 29** — on `KEEP = 60`

> how many copies to hold, newest first
